# Dev-only deps for the webapp (tests + lint/format). Install into the same venv:
#   webapp/.venv/bin/pip install -r webapp/requirements-dev.txt
# Versions align with the stack already present in the venv (pytest 9.x + pytest-asyncio).
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
respx==0.22.0
ruff==0.8.6

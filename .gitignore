node_modules/

webapp/.venv/
webapp/__pycache__/
webapp/**/__pycache__/
webapp/.env

data/

logs/
*.log

.DS_Store
Icon?
.env

# Tool caches (Ruff writes its own .ruff_cache/.gitignore too, but be explicit)
.ruff_cache/

# Local Claude Code settings (shared .claude/settings.json may be committed; local overrides are not)
.claude/settings.local.json

_old/

node-red/flows_cred.json
node-red/.config.*.json
node-red/.flows.json.backup

media/*
!media/00 qlab workspaces/
!media/00 qlab workspaces/**
*.qlab5backup

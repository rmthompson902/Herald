# QLab Scheduled Audio Messaging System — Implementation Plan

## Context

`project-brief.md` lays out a scheduling layer for QLab: operators need recurring/time-boxed audio messages (safety announcements, closing messages, etc.) triggered across "zones" of a venue's audio system, without hand-building every playback event in QLab. The brief proposed Node-RED + cron-plus + SQLite + OSC as the stack, with QLab remaining the sole owner of playback/audio-routing.

This is a **greenfield project** — the repo currently contains only the brief. Before writing any code, we ran an extensive design interview to resolve every open architectural fork in the brief (platform choice, licensing constraints, zone modeling, collision handling, emergency messaging, health monitoring, auth). Those decisions are final and drive everything below; this plan is the concrete build-out of them.

Two decisions from that interview are worth calling out because they overturned the brief's original framing:
- **Zones have no manual configuration.** The venue routes every QLab cue to the same Dante Virtual Soundcard; "zone" is defined entirely by which output channels a cue's QLab-side level is set to 0dB vs. -inf. We discovered QLab's OSC dictionary exposes this per-cue (`/cue/{id}/levels`), so the system derives zone membership automatically instead of operators tagging it in the dashboard.
- **Health monitoring/feedback, initially considered a "later phase" feature** (out of concern that it needed the paid QLab license we don't have on the dev machine yet), **turned out to be fully buildable now** — QLab 5's official docs confirm OSC remote control, Network cues, and update feeds are all available on the free/unlicensed tier. So it's pulled into v1 in full, along with a `/thump`-heartbeat-driven connectivity monitor and a queue-and-wait collision engine backed by real QLab confirmation (not just timers).

## Frontend Pivot: Dashboard 2.0 → FastAPI (mid-implementation, after Phase 3)

Phases 0-2 and the non-UI half of Phase 3 (Node-RED scheduling engine, live-validated against real QLab) went smoothly and are solid. Building the operator dashboard in `@flowfuse/node-red-dashboard` (Dashboard 2.0) did not: every widget's dynamic-update behavior had to be reverse-engineered from the compiled frontend bundle (no way to use Dashboard 2.0's own visual editor or see a browser from here), producing repeated rounds of guess → user tests in a real browser → wrong guess → fix, including a framework-level bug (no `<base href>`, breaking any page refresh except the bare dashboard root) that isn't fixable from our side at all.

**Decision: drop Dashboard 2.0 entirely. Node-RED becomes a headless scheduling/OSC engine with a small internal JSON API (plain `http-in`/`http-response` nodes, not a UI framework). A new, separate Python/FastAPI web application is the actual operator-facing multi-page frontend**, modeled on an existing app the user has built and reused repeatedly (`/Users/ryanthompson/Documents/_dev/UPS-Mgmt/`: Flask + Jinja2 server-rendered templates, vendored Bootstrap 5 + Font Awesome, a shared `base.html` shell with `{% block %}` extension points, Jinja2 macros for reusable components, vanilla ES6 JS "manager" utility classes, a standardized JSON API envelope).

This is a UI-layer swap only. **None of the `lib/` business logic changes** — every module in the Repo Structure below is unchanged and still runs inside Node-RED exactly as built and validated. See "Frontend Architecture" below for the concrete split.

**Nothing from Phases 0-3 is discarded.** All code, tests, and fixtures from those phases stay exactly as built — this pivot only removes the `@flowfuse/node-red-dashboard` flow nodes (never the engine nodes) and adds a new `webapp/` directory alongside, it doesn't touch `lib/`, `test/`, or the proven `fn_startup`/`cronplus1`/`fn_on_due` flow. The Build Phases section below keeps Phases 0-3 listed with their original detail, marked done, specifically so that history stays referenceable rather than being summarized away.

**Archiving the Dashboard 2.0 attempt, not deleting it.** Before stripping `node-red/flows.json` down to its engine-only content (Phase 5), copy the current full file to `_old/node-red-flows-dashboard2-attempt.json` (a new gitignored `_old/` directory at repo root — add it to `.gitignore`). This preserves the widget configuration we reverse-engineered (schema quirks, working `selectionType`/`ui_update` shapes, etc.) as a local reference in case anything is useful later, without it cluttering the tracked repo or implying it's still in use. Same treatment for anything else in this pivot worth keeping-but-not-tracking as it comes up.

## Confirmed Decisions (do not re-litigate during implementation)

- **Hosting:** Node-RED, self-hosted on the same Mac as QLab, communicating over local OSC. No FlowFuse cloud/platform. Node-RED is now **headless** (no dashboard UI of its own) — see Frontend Architecture below. Its HTTP API binds to `127.0.0.1` only, since nothing but the co-located FastAPI app needs to reach it.
- **Auth:** none, anywhere (Node-RED's API, FastAPI's pages/API). Access is gated entirely by KVM + a locked-down LAN/firewall perimeter.
- **Design split:** Node-RED hosts cron-plus triggering and OSC/SQLite plumbing; FastAPI hosts the operator-facing multi-page UI and browser-facing API. All non-trivial logic (collision engine, VOG, health monitor, zone derivation, schedule validation, cron-plus sync) lives in plain, unit-testable JS modules in `lib/` that Node-RED Function nodes call into — not sprawling flow wiring, and never reimplemented in Python.
- **QLab licensing:** dev machine is currently unlicensed; production machine gets a purchased QLab Audio license eventually. Verified (docs) that OSC control/query/update-feed functionality works on the free tier, so real hands-on testing is expected to work now — no mock QLab responder needed. This should be the very first thing validated (Phase 0 below), since everything else depends on it.
- **Cues:** pre-built by the operator in QLab under a stable naming convention (e.g. `MSG.LOBBY.SAFETY`, `MSG.ALL.EMERGENCY`). Node-RED only triggers/queries cues by name — it never creates or edits them.
- **Duration:** sourced live from QLab via `/cue/{id}/duration` (accounts for trim/repeat). Never entered manually.
- **Zones:** derived automatically from `/cue/{id}/levels` (per-cue output-channel level matrix) cross-referenced against one small manual file, `config/zone-map.json` (Dante channel number → zone name — the *only* manual config in the system, since it reflects fixed physical wiring). The webapp shows the derived zone(s) back to the operator, read-only.
- **Collision handling:** queue-and-wait, per zone — never skip, never overlap. A newly-due cue waits for the zone's current occupant to finish (tracked via QLab-sourced duration **and** live `/updates` confirmation, not a blind timer alone). If a schedule's own next occurrence comes due while a prior instance of the *same* schedule is still queued, the stale one is dropped (consistent with the brief's existing "skip missed events, don't replay in bulk" principle). Cross-schedule ordering in the same zone: FIFO by due time. "Play-now" (the webapp's test button) goes through this exact same queue rather than bypassing it — since "nothing should overlap" is a hard requirement, a UI convenience button isn't allowed to be the one exception. It surfaces a toast if it ends up waiting behind something already playing.
- **VOG (Voice of God):** a single emergency priority tier — one or more webapp-only, manually-triggered cues (never scheduled). Triggering one stops every active cue within that VOG cue's own auto-derived zone scope (one zone, several, or all — same derivation mechanism as any other cue), then plays the VOG message. Anything it interrupted (including queued entries) is dropped afterward, not resumed.
- **Health monitoring (in v1, not deferred):** subscribe to QLab's `/updates` feed for real-time cue-state/disconnect events; keep the UDP registration alive via periodic `/thump` + `/udpKeepAlive true` (QLab drops idle UDP OSC clients after 61s); a few consecutive missed heartbeats flips a "disconnected" state. The webapp's Status page shows a visual alarm — no external paging. This same connectivity state gates the scheduler: it stays disarmed until QLab is confirmed live (satisfies the brief's startup-gate requirement). No workspace-identity matching is needed on top of this — only one workspace file will ever run on the deployed machine, so "any QLab is responding" is sufficient; there's no "wrong show file open" case to guard against.

## Stack (verified against the npm registry)

| Package | Version | Role |
|---|---|---|
| `node-red-contrib-cron-plus` | 2.2.4 | recurring/time-based scheduling engine |
| `osc` | 2.4.5 | OSC-over-UDP library, used directly in a custom client rather than a generic Node-RED OSC node — needed for request/response correlation on QLab's query-style addresses |
| `better-sqlite3` | 12.11.1 | sync SQLite driver — fits Function nodes cleanly, no async ceremony for simple CRUD |
| `winston` | 3.19.0 | app-diagnostic logging — direct equivalent of Python's `logging` module |
| `winston-daily-rotate-file` | 5.0.0 | midnight-rotating log files with retention — equivalent of `TimedRotatingFileHandler` |

`@flowfuse/node-red-dashboard` is removed (see Frontend Pivot above) — uninstall it and delete its flow nodes.

**New: `webapp/` Python stack** (versions to be pinned in `webapp/requirements.txt` when scaffolded — check current versions at that time rather than trusting these as final):

| Package | Role |
|---|---|
| `fastapi` | web framework — pages (via `Jinja2Templates`) + browser-facing JSON API |
| `uvicorn[standard]` | ASGI server |
| `jinja2` | server-rendered templates, same pattern as the UPS-Mgmt reference app |
| `pydantic` (bundled with FastAPI) | request/response models for the API |
| `httpx` | async HTTP client for FastAPI → Node-RED proxy calls |
| `python-multipart` | required by FastAPI for HTML form submissions |
| `python-socketio` | real-time push to the browser — a background thread polls Node-RED's `/api/health` (~2s interval, mirroring UPS-Mgmt's own daemon-thread polling pattern) and re-broadcasts state changes; the browser never talks to Node-RED directly |

## Repo Structure

```
lib/                        # plain, testable JS — no Node-RED coupling except at two edges
├── index.js                 # composition root; facade exposed to Node-RED via functionGlobalContext
├── osc/
│   ├── oscClient.js          # raw UDP transport + request/response correlation + timeouts
│   └── qlabProtocol.js       # QLab verbs: getDuration, getLevels, playCue, stopCue, listCues, subscribeUpdates, thump
├── zones/
│   ├── zoneMap.js            # loads/validates config/zone-map.json
│   └── zoneResolver.js       # parseLevelsMatrix (pure) + resolveZonesForCue (composed w/ IO)
├── scheduling/
│   ├── scheduleModel.js      # pure validation/normalization of schedule input
│   ├── occurrenceCalculator.js  # pure: next-occurrence math, cron-plus job-spec builder, DST-safe active-window check
│   └── cronSync.js           # diffs SQLite schedules vs. cron-plus job specs
├── queue/
│   └── zoneQueueEngine.js    # per-zone occupancy + FIFO wait queue + stale-drop + VOG preempt (pure/in-memory)
├── vog/
│   └── vogInterruptHandler.js
├── health/
│   └── healthMonitor.js      # heartbeat state machine, /updates lifecycle, scheduler-arm gate
├── log/
│   ├── appLogger.js          # winston + winston-daily-rotate-file; mirrors the user's reference Python setup
│   │                         #   (console + file, "name - level - message", midnight rotation, 30-day retention)
│   └── eventLogger.js        # separate daily-rotating plain-text log, one line per business event
│                              #   (fired/queued/dropped_stale/vog_interrupt/vog_fired/play_now/health_*),
│                              #   30-day retention — feeds the dashboard's Event History view
└── db/
    ├── database.js
    ├── migrations/001_init.sql (+ runner.js)
    └── repositories/{schedulesRepo,vogMessagesRepo,cueCacheRepo}.js

node-red/                   # Node-RED userDir (flows.json, settings.js wiring functionGlobalContext.core = require('../lib'))
                              #   headless: no dashboard UI, just the scheduling engine + a small internal HTTP API
                              #   (uiHost bound to 127.0.0.1 - see Frontend Architecture)
config/zone-map.json        # the one manual config file
config/env.example           # QLAB_OSC_HOST, QLAB_OSC_PORT, DB_PATH, NODE_RED_API_PORT
data/schedule.db            # gitignored, created by migration runner - shared by Node-RED (read/write) and
                              #   webapp/ (read-only), safe under SQLite's WAL mode (already enabled)
logs/                       # gitignored; app-YYYY-MM-DD.log + events-YYYY-MM-DD.log, both 30-day auto-pruned
                              #   - shared: webapp/'s History page tails events-*.log directly, no proxy needed
test/unit/                  # jest, mirrors lib/ structure
test/fixtures/              # real captured OSC payloads from the Phase 0 spike
test/integration/INTEGRATION_CHECKLIST.md   # hands-on checklist against real QLab
docs/adr/0001-zone-queue-tiebreak-policy.md
_old/                       # gitignored: archived Dashboard 2.0 flows.json (see Frontend Pivot) and anything
                              #   else worth keeping locally but not tracking - never referenced by running code

webapp/                     # NEW: the actual operator-facing frontend (FastAPI). See Frontend Architecture below.
├── requirements.txt
├── .env.example             # NODE_RED_API_BASE, DB_PATH, PORT
├── app/
│   ├── main.py               # FastAPI() app, routers, StaticFiles, Jinja2Templates
│   ├── config.py             # pydantic-settings, mirrors lib's env-var pattern
│   ├── node_red_client.py     # thin wrapper around httpx calls to Node-RED's internal API - the ONE place
│   │                          #   that knows Node-RED's endpoint shapes; handles connect-refused gracefully
│   ├── db/queries.py          # read-only sqlite3 SELECT helpers mirroring schedulesRepo/vogMessagesRepo/
│   │                          #   cueCacheRepo's read shapes - no writes ever happen from here
│   ├── models/{schedule,vog}.py   # Pydantic request/response models (client-side UX validation only -
│   │                          #   Node-RED's validateSchedule remains the actual authority on every write)
│   └── routers/{pages,schedules_api,vog_api,cues_api,status_api}.py
├── templates/
│   ├── base.html              # shared shell, ported from UPS-Mgmt's base.html pattern
│   ├── schedules/{list,form}.html
│   ├── vog/{list,form}.html
│   ├── history.html, status.html
│   └── partials/components/{status_badge,action_button,modal_base}.html   # ported macros
└── static/
    ├── vendor/{bootstrap,fontawesome}/   # copied from UPS-Mgmt/static/vendor/
    ├── css/style.css
    └── js/
        ├── utils/{api-client,modal-manager,button-state-manager}.js   # ported as-is from UPS-Mgmt
        └── {schedules_list,schedule_form,vog_list,vog_form,status}.js
```

## SQLite Schema

- **`schedules`** — name, `qlab_cue_number`, interval, start/end time, weekdays (JSON), date range, enabled flag.
- **`vog_messages`** — name, `qlab_cue_number`, enabled flag. Deliberately no timing fields (manual-trigger only).
- **`cue_cache`** — cached-only (never authoritative): duration, derived zones (JSON), QLab's internal cue id (needed to reconcile `/updates` events, which are keyed by internal id, not cue number — confirm the lookup OSC address, likely `/cue/{number}/uniqueID`, during Phase 0), refreshed_at.

There is no `event_log` table — business event history lives in `logs/events-YYYY-MM-DD.log` instead (see Logging below), not SQLite. This keeps the schema to exactly the durable configuration the operator actually edits through the dashboard.

Explicitly **not** persisted in SQLite: connectivity state and the live zone-occupancy queue. Both reset cleanly on restart by design — cron-plus jobs are fully rebuilt from `schedules` on boot, and losing in-flight queue state on restart is correct given the "skip, don't replay" principle. SQLite + `zone-map.json` are the only real sources of truth for durable configuration.

## Logging

Two separate daily-rotating plain-text logs, both via `winston` + `winston-daily-rotate-file`, both in `logs/`, both with 30-day auto-prune (mirroring the retention/rotation style of the user's reference Python `TimedRotatingFileHandler` setup, adapted to Node's idioms — a per-module logger name substitutes for Python's `%(funcName)s:%(lineno)d`, which isn't cheaply available in Node without stack-trace parsing):

- **`app-YYYY-MM-DD.log`** (`lib/log/appLogger.js`) — general application diagnostics for troubleshooting the system itself: console (configurable level, default INFO) + file (DEBUG, detailed format), one named child logger per `lib/` module.
- **`events-YYYY-MM-DD.log`** (`lib/log/eventLogger.js`) — the business/operator-facing history: one consistent plain-text line per domain event (`fired` / `queued` / `dropped_stale` / `vog_interrupt` / `vog_fired` / `play_now` / `error` / `health_disconnect` / `health_reconnect`), human-readable directly in a text editor and simple enough for the webapp's Event History page to parse. This is what the webapp tails/reads for "event history"; older days are just older files, which the operator manages (archives/deletes) directly on disk — no in-app retention UI.

## Frontend Architecture: FastAPI ↔ Node-RED split

**Rule: FastAPI reads SQLite directly (read-only) for everything displayable. Every write to `schedules` or `vog_messages`, and everything requiring the live OSC connection, proxies to Node-RED.** This keeps `validateSchedule` and `cronSync.syncOne` single-sourced in `lib/` (the whole point of the pivot) while not making simple page-load reads depend on a second process being up.

| Operation | Path |
|---|---|
| List/get schedules, VOG messages, cue_cache | FastAPI → SQLite directly (read-only connection; WAL mode already enabled makes this safe alongside Node-RED's writes) |
| Create/update/delete/toggle-enabled schedule or VOG message | FastAPI → proxy to Node-RED (`validateSchedule` + `cronSync.syncOne` must run there) |
| Cue browsing (`listCues`), play-now, VOG trigger, connection health | FastAPI → proxy to Node-RED (owns the live OSC socket) |
| Next-occurrence display | FastAPI → proxy to a bulk Node-RED endpoint (pure `occurrenceCalculator` math, but DST-safe logic must not be reimplemented in Python) — best-effort, page never blocks on it |
| Event history | FastAPI reads `logs/events-YYYY-MM-DD.log` directly — plain text, no proxy needed |

Pydantic models in `webapp/app/models/` mirror `scheduleModel.js`'s field rules (HH:MM regex, weekdays 1-7, etc.) for instant client-side feedback only — this is UX duplication, not business-logic duplication. Node-RED's `validateSchedule` still runs on every write and remains the actual authority; a passing Pydantic check never skips the proxy call.

**Node-RED's internal HTTP API** (`http-in`/`http-response` nodes only — plain, well-documented, not a UI framework; bound to `127.0.0.1`):

| Method | Path | Backing call |
|---|---|---|
| `POST` | `/api/schedules` | `validateSchedule` → `schedulesRepo.create` → `cronSync.syncOne` → apply to `cronplus1` |
| `PUT` | `/api/schedules/:id` | same, via `schedulesRepo.update` |
| `DELETE` | `/api/schedules/:id` | `cronSync.toRemoveCommand` applied → `schedulesRepo.remove` |
| `POST` | `/api/schedules/:id/toggle` | `schedulesRepo.setEnabled` → `cronSync.syncOne` → apply |
| `GET` | `/api/schedules/next-occurrences` | loops `schedulesRepo.listEnabled` through `occurrenceCalculator` (~48h window) |
| `POST` / `PUT` / `DELETE` / `.../toggle` | `/api/vog-messages[/:id]` | mirrors schedule CRUD via `vogMessagesRepo` (no cron-plus sync — VOG has no timing) |
| `POST` | `/api/vog-messages/:id/trigger` | gated on `core.health.isArmed()`; calls `vogInterruptHandler` once Phase 6 lands, `qlabProtocol.playCue` as an interim stub until then — **contract is stable now, internals upgrade in place later** |
| `GET` | `/api/cues` | `qlabProtocol.listCues()` live |
| `POST` | `/api/schedules/:id/play-now` | same interim-stub note — upgrades to go through `zoneQueueEngine` once Phase 5 lands |
| `GET` | `/api/health` | `core.health.getState()` / `isArmed()` |

Each write endpoint needs the same "turn a `cronSync` directive into a real `cronplus` control message" translation currently inline in `fn_startup` — extract it into one shared helper (e.g. `node-red/lib/flows/applyCronSyncDirectives.js`) so `fn_startup`'s `rebuildAll` path and the new per-endpoint `syncOne` calls share identical logic instead of two slightly-different copies.

**Multi-page breakdown** (mirrors the UPS-Mgmt "list page + separate add/edit page" pattern):

1. `GET /schedules` — list: name, cue, timing summary, derived zones/duration (from `cue_cache`), enabled toggle, next-occurrence, row actions (Edit/Play Now/Delete/Enable-Disable).
2. `GET /schedules/new` and `GET /schedules/{id}/edit` — one shared form template (`schedule=None` vs. populated), cue-picker backed by `GET /api/cues`.
3. `GET /vog` — same list pattern, with a "Trigger" action instead of timing fields.
4. `GET /vog/new` / `GET /vog/{id}/edit` — same form pattern, no timing/weekday/date-range fields.
5. `GET /history` — tails `logs/events-YYYY-MM-DD.log`.
6. `GET /status` — QLab connection state/armed flag, updated in real time via the SocketIO relay (see below).

Nav in `base.html`: Schedules / VOG Messages / Event History / Connection Status.

**Files to port from UPS-Mgmt verbatim, then adapt** (mirror its feel/style directly, not just its structural pattern — copy these into `webapp/` and edit in place, don't rewrite from scratch):

| Source (`/Users/ryanthompson/Documents/_dev/UPS-Mgmt/`) | Destination (`webapp/`) | Adaptation needed |
|---|---|---|
| `templates/base.html` | `templates/base.html` | Swap nav items (Dashboard/Devices/Settings → Schedules/VOG/History/Status), swap branding strings, keep the `{% block title/additional_css/content/scripts %}` shell, toast system, and `socket = io()` connection as-is (real-time push is confirmed, not dropped — see below) |
| `templates/components/action_button.html`, `templates/partials/components/{modal_base,modal_wrapper,page_header,status_badge}.html` | `templates/partials/components/` | Mostly copy as-is; `status_badge` needs new status→color/icon mappings for schedule-enabled/connection-state instead of device/battery status |
| `static/css/style.css` | `static/css/style.css` | Copy the design-token system (dark theme CSS custom properties) as-is; this is exactly "mirror the feel/style" |
| `static/vendor/bootstrap/`, `static/vendor/fontawesome/` | `static/vendor/` | Copy verbatim, no changes — same offline-capable vendoring approach |
| `static/js/utils/api-client.js` | `static/js/utils/api-client.js` | Copy the base `APIClient` class as-is; replace `DeviceAPI`/`ProfileAPI`/`SystemAPI` subclasses with `ScheduleAPI`/`VogAPI`/`CueAPI`/`StatusAPI` hitting this app's own `/api/*` routes |
| `static/js/utils/modal-manager.js`, `button-state-manager.js`, `tooltip-manager.js`, `constants.js` | `static/js/utils/` | Copy as-is; `constants.js`'s `window.AppConstants` object gets this app's own modal IDs/event names/messages |
| `src/utils/response_helpers.py` | `app/responses.py` | Copy the `success_response()`/`error_response()` JSON envelope convention as-is |
| `src/config.py`'s `get_env()` pattern | `app/config.py` | Same typed env-var helper pattern, new var names (`NODE_RED_API_BASE`, `DB_PATH`, `PORT`) |

**Not** porting: `Flask-SocketIO`/the live `socket = io()` connection in `base.html`, `src/custom_html.py`/`src/custom_css.py` (operator-configurable branding-override system — real feature for a multi-deployment product, not needed for this single-venue tool), the domain-specific `src/ups/*` logic and its templates/JS (obviously), and the `old/` legacy-reference folder within UPS-Mgmt itself (not relevant here).

**Real-time push (confirmed, not polling):** FastAPI runs `python-socketio` and a background thread mirroring UPS-Mgmt's own daemon-thread polling pattern — it polls Node-RED's `GET /api/health` internally (every ~2s) and re-broadcasts state changes to connected browsers over its own socket, exactly like `base.html`'s `socket = io()` connection in the reference app. The browser never talks to Node-RED directly; FastAPI is the only relay.

**Landing page:** `GET /` and `GET /schedules` render the same content — the schedule list *is* the landing page (no separate summary/overview page). Whether the template file is literally named `dashboard.html` or `schedules.html` doesn't matter.

**Template generalization approach (confirmed):** no separate up-front "extract an abstract template" project. Generalize in place while porting each file from UPS-Mgmt into `webapp/` — rename any UPS/battery-specific identifiers to generic ones as they're copied, rather than leaving domain-specific names hacked to fit. The result (this app's `base.html`/macros/CSS/JS utils) becomes the reusable template for future projects, proven across two real apps instead of designed abstractly.

Explicitly **not** in v1: any authentication, manual zone tagging, configurable per-schedule overlap policy (the brief's original "skip / queue / interrupt" options) — collision handling is a single fixed queue-and-wait behavior, and VOG is the only interrupt mechanism, full stop.

## Reference documentation

Research for this plan was done against **QLab 5** docs specifically — confirm this matches the actual QLab version in use (see Open Items below):
- OSC dictionary: https://qlab.app/docs/v5/scripting/osc-dictionary-v5/
- OSC queries: https://qlab.app/docs/v5/scripting/osc-queries/
- Using OSC with QLab: https://qlab.app/docs/v5/networking/using-osc/
- Features by license type: https://qlab.app/docs/v5/general/features/

## Zone-Queue Tie-Break Policy (judgment call — recorded as `docs/adr/0001-zone-queue-tiebreak-policy.md`)

1. One FIFO queue per zone.
2. Zone frees (via `/updates` confirmation or expected-end fallback timer) → pop and fire the front entry.
3. Enqueuing a schedule that already has an entry waiting in that zone → drop the old entry, treat this as the live occurrence (stale-drop, not double-stack).
4. Cross-schedule ordering: FIFO by due time; exact ties broken by schedule id.
5. Defensive cap of ~5 queued entries per zone (oldest dropped + logged beyond that) as a safety net against a stuck QLab — cheap insurance, not explicitly requested.

VOG preemption clears zone occupancy and drops all waiting entries outright for its target zones — no requeue.

## Build Phases

**Phases 0-2 are complete** — see status notes inline. Phase 3 is split: its engine half (0-2's natural continuation) is done and live-validated; its original dashboard half is replaced by the phases below per the Frontend Pivot.

0. ✅ **QLab OSC spike** — done. Validated on real free-tier dev QLab: reply envelope shape, `-inf` = -60dB, cue-list enumeration, `/updates` push behavior + cue-id keying (not cue number — needs `cue_cache.qlab_internal_id`), `/thump` + `/udpKeepAlive` past the 61s timeout, no unlicensed-tier nag dialog. Findings captured in `test/fixtures/qlab-osc-findings.md`.
1. ✅ **Skeleton + persistence** — done. Migrations, repos, `zone-map.json` + loader, all tested against real temp SQLite files.
2. ✅ **OSC transport + protocol layer** — done. `oscClient` + `qlabProtocol` built from Phase 0 findings and live-validated; caught and fixed a real bug (`/cue/.../start`/`stop` only reply on denial, success is silent — `requestOptionalReply()`). `healthMonitor` state machine tested with fake timers and live-validated (heartbeat, keepalive, disconnect detection).
3. ✅ **Node-RED scheduling engine** — done and live-validated repeatedly: `scheduleModel`, `occurrenceCalculator` (including a real DST-collapse dedup bug found and fixed), `cronSync` (built against cron-plus's actual source — uses `expressionType: "dates"` with a computed window of upcoming timestamps, not a hand-built cron expression, since arbitrary-second intervals can't be represented in cron syntax), and `lib/index.js`'s composition root. A seeded schedule has fired repeatedly through cron-plus → real QLab playback, unattended.
4. ✅ **Zone derivation** — done. `zoneResolver.parseLevelsMatrix` implements the confirmed two-stage gating logic, live-validated. `cue_cache` population wired into both the startup warm-up (`node-red/lib/refreshCueCache.js`, refreshes every cue referenced by any schedule or VOG message) and the schedule/VOG create+update handlers — verified live: cue 101 correctly cached as Zone 1 only (the gated case), cues 102-104 cached correctly, and the create-path refresh confirmed via a fresh `refreshedAt` timestamp.
5. ✅ **Node-RED internal HTTP API** — done. Archived the pre-pivot `flows.json` to `_old/node-red-flows-dashboard2-attempt.json` (gitignored). All 13 endpoints (`schedules` CRUD+toggle+next-occurrences, `vog-messages` CRUD+toggle+trigger, `cues`, `play-now`, `health`) built as `http-in → function → http-response` chains in a new `tab_api` tab, sharing `node-red/lib/applyCronSyncDirectives.js` with the startup rebuild path. `@flowfuse/node-red-dashboard` uninstalled. `settings.js` binds `uiHost: '127.0.0.1'` (confirmed via `lsof`: loopback only). Every endpoint verified live via `curl` — full CRUD round-trips, 400/404 error paths, VOG trigger and play-now both actually played audio on QLab, and the engine's own 30s recurring fire kept working unaffected throughout.
   - **Post-Phase-6 bugfix:** while exercising the toggle button from the new webapp, found that `POST /api/schedules/:id/toggle` and `/api/vog-messages/:id/toggle` weren't real toggles - they trusted an `enabled` flag from the request body (`!!(msg.payload && msg.payload.enabled)`), so a client that calls them with no body (both the webapp's JS and the FastAPI proxy do) always set `enabled: false`. Disable appeared to work every time; enable never did. Fixed `fn_toggle_schedule`/`fn_toggle_vog` in `node-red/flows.json` to look up the row and flip its current `enabled` state server-side instead. Verified by toggling schedule #1 and VOG #1 three times each via `curl` - flips correctly every time now. Required restarting the local Node-RED dev process to pick up the flows.json change (it doesn't watch the file for external edits).
6. ✅ **FastAPI webapp scaffold** — done. `webapp/` scaffolded per Repo Structure: `app/{main,config,node_red_client,responses,templating}.py`, `app/db/queries.py` (read-only, mirrors the repos' field shapes), `app/models/{schedule,vog}.py` (Pydantic UX validation mirroring `scheduleModel.js`'s rules), `app/routers/{pages,schedules_api,vog_api,cues_api,status_api}.py`. Every file in the "Files to port from UPS-Mgmt" table was copied and adapted in place: `base.html` (nav swapped to Schedules/VOG/History/Status, UA branding/custom_html system dropped, toast + `socket = io()` kept), the five partials/components macros (`status_badge` remapped to schedule/connection/armed/queue types instead of device/battery/profile), `style.css` (design-token system kept, UPS-only component styles like transport-controls/clips-list dropped, accent color changed from UA crimson to a neutral blue since that hex was literally UA branding — flagged for the user, easy to revert), `api-client.js` (`ScheduleAPI`/`VogAPI`/`CueAPI`/`StatusAPI` replacing `DeviceAPI`/`ProfileAPI`/`SystemAPI`), `button-state-manager.js`/`modal-manager.js`/`tooltip-manager.js` (as-is), `constants.js` (rewritten for this domain), `response_helpers.py` → `responses.py`, `config.py`'s `get_env()` pattern → `pydantic-settings`-based `config.py`. One genuinely new element not in UPS-Mgmt: a `.btn-vog` pulsing-alarm button style (reusing the existing pulse-animation CSS custom properties) reserved for the VOG trigger action, so the one control that stops audio and interrupts the venue reads as "alarm" rather than an ordinary danger button. Full page set built (list/new/edit for schedules and VOG, history, status), not just the empty-state scaffold literally described here, since the nav requires all of them to resolve. Verification: venv created, all deps installed cleanly; every page (`/`, `/schedules`, `/vog`, `/history`, `/status`, `/schedules/new`, `/schedules/{id}/edit`, `/vog/new`, `/vog/{id}/edit`) returns 200 with correct real data (schedule for cue 101, VOG message for cue 104 with zones 1+2 correctly derived) and zero server errors; confirmed live end-to-end with Node-RED/QLab actually running (play-now/cue-list proxy calls worked); separately confirmed the "reads never depend on Node-RED" design point by pointing `NODE_RED_API_BASE` at an unreachable port — `/schedules` still rendered full real data, `/status` showed a clean "Node-RED unreachable" message, `/api/cues` returned a clean 503 envelope instead of crashing. Found and fixed one real bug in the process: optional schedule fields (`start_time`/`end_time`/date range) rendered the literal text "None" in edit-form inputs due to a Jinja ternary only guarding on `schedule` being falsy, not on the field itself being `None`.
7. ✅ **Schedules + VOG pages** — done. Phase 6 had already built functional list/add/edit pages and basic wiring (play-now, toggle, delete, create/update); this phase closed the two real interaction gaps left open at the time: (1) a custom `CuePicker` component (`static/js/utils/cue-picker.js` + `.cue-picker*` CSS) replacing the native `<datalist>` cue field on both forms - searchable, keyboard-navigable, themed to match the app instead of showing raw browser chrome; (2) a "Next Fire" column on the schedules list, wired to `GET /api/schedules/next-occurrences` (best-effort, non-blocking, shows `in Ns (clock time)` for enabled schedules within the hour). Also gave the VOG trigger action its own dedicated confirmation modal (`#vogTriggerModal` in `templates/vog/list.html`) instead of the generic bootstrap confirm dialog - a quiet red-accent border (`.vog-modal-content`, reusing the `--vog` token from Phase 6) around an otherwise ordinary dialog, keeping the pulsing-alarm treatment reserved for the trigger button alone so it doesn't read as noisy. Verification: full schedule create → update (disable) → delete round-trip run live against the running Node-RED/QLab instance - confirmed via Node-RED's log that the test schedule (45s interval) actually fired via cron-plus after creation, stopped firing immediately after the disable-update, and disappeared from `next-occurrences` after delete. Toggle round-trip (both directions) was already verified live during the post-Phase-6 bugfix above.
   - **Post-Phase-7 round of fixes**, from hands-on user testing:
     - **Live next-fire countdown**: the "Next Fire" cell now re-renders every second client-side from an already-fetched timestamp (no added server load), resyncing with the server every 15s. Found and fixed a real bug in the first version: once the countdown hit zero it froze on the stale past timestamp instead of continuing, because the cached occurrence was already in the past and nothing forced an early resync. Fixed by showing a transient "firing now…" state and having the per-second tick force an immediate resync (throttled to once per 3s) whenever any row's cached occurrence has passed.
     - **Cue cache freshness**: added both a Node-RED periodic background sweep (`fn_periodic_cue_refresh`, every 5 minutes, sharing the new `refreshAllReferencedCues` helper in `node-red/lib/refreshCueCache.js` with `fn_startup`) and a manual path. The manual path started as a per-row refresh icon, then per user feedback was replaced with a single global "Refresh Cue Data" button baked into the shared `page_header` macro (`partials/components/page_header.html`) so it appears identically on every page, top-right under the nav, left of each page's primary action - `POST /api/cues/refresh-all` end to end (Node-RED → FastAPI proxy → `CueAPI.refreshAllCues()`). Verified the periodic sweep live: waited 5+ minutes and confirmed all three referenced cues' `refreshed_at` timestamps advanced together automatically, while an unreferenced cue was correctly left untouched.
     - **VOG toggle didn't gate the trigger at all**: `POST /api/vog-messages/:id/trigger` never checked `enabled`, only existence and QLab connectivity - so a disabled message could still be triggered. Per user's chosen design (enabled *is* armed, no separate confirmation), the endpoint now rejects triggering a disabled message (400), the confirmation modal was removed entirely, the Trigger button server-renders `disabled` (no pulse) when disarmed, and the status badge reads "Armed"/"Disarmed" for VOG specifically via a new `vog_armed_status_badge` macro. Verified live in both directions.
     - Confirmed with the user: VOG trigger still doesn't actually stop other cues in its zones yet - that's `vogInterruptHandler` (Phase 9), not a bug in this phase's scope.
8. ✅ **Queue/collision engine** — done. `lib/queue/zoneQueueEngine.js` built: per-zone FIFO wait queues + occupancy tracking, admission requires *every* one of a multi-zone entry's target zones to be simultaneously free and at the front of each zone's own queue (not just N independent per-zone FIFOs - see `docs/adr/0001-zone-queue-tiebreak-policy.md`, written this phase since it had only ever existed as a plan section, not an actual file), stale-drop keyed on an explicit `dedupeKey` set only on cron-plus schedule fires (never play-now/VOG, so those can't stale-drop or be stale-dropped), a 5-per-zone overflow cap dropping the oldest waiting entry, and a zero-zone fast path (a fully-muted/unrouted cue has nothing to collide over, so it fires immediately rather than being pushed into no queue and hanging forever - a real gap caught before it shipped, not just theoretical). A zone frees via whichever comes first: a live `/updates` push confirming the occupant actually stopped (new `qlabProtocol.getIsRunningByUniqueId`, addressed via QLab's `/cue_id/{uniqueId}/...` scheme rather than `/cue/{number}/...`) or a duration-based fallback timer seeded from a live (not cached) `refreshCueCache` call made at admission time. `lib/index.js`'s `createCore` wires `oscClient`'s `message` events for `/update/workspace/.../cue_id/{id}` straight into the engine and exposes it as `core.queue`. `fn_on_due` (cron-plus schedule fires) and `fn_play_now` (the webapp's play-now endpoint) both upgraded from their interim direct-`playCue` stubs to `core.queue.enqueue(...)`, matching contracts already in place since Phase 6 (`{queued: true/false}` in the play-now response, already correctly toasted by the Phase 7 frontend with no changes needed there).
   - Unit tests (`test/unit/zoneQueueEngine.test.js`, fake timers, 12 cases): immediate-fire, occupied-queues-and-later-fires, same-schedule stale-drop, non-schedule entries never stale-drop, cross-schedule FIFO ordering, overflow cap drops oldest, multi-zone entries wait on every target zone, early free via a confirmed `/updates` stop, ignoring updates for untracked uniqueIds, `preemptZones()` clearing occupancy/queue without requeueing (built now for Phase 9 to call, unused until VOG actually preempts), zero-zone fast path, and a read-only `getState()` snapshot. Also added a live-spike-verified unit test for `qlabProtocol.getIsRunningByUniqueId`. All 71 project unit tests pass.
   - Live verification against the real running Node-RED/QLab instance (not just curl-level): confirmed `/cue_id/{uniqueId}/isRunning` is a real, working QLab 5.6.2 address (previously unverified/unspiked) - discovered along the way that QLab only replies to a UDP client on the specific local port it's already registered to (explains an initial silent-failure spike attempt against an ad-hoc port; the orphaned `/cue/101/start` that attempt actually fired for real was cleanly stopped afterward). Two truly-concurrent `play-now` calls on the same cue produced exactly one immediate fire and one queued, confirmed via response bodies. Two `play-now` calls on different-zone cues (101/Zone 1, 102/Zone 2) both fired immediately, confirming zones don't block each other. Deliberately saturating both zones via manual testing caused the next automatic cron-plus due-fires for both schedules to correctly log "Queued" instead of "Fired" in Node-RED's own log, and both later logged "Fired" again once their zone's turn came - confirming queued entries are never dropped or stuck, and normal operation resumes cleanly. Restarted Node-RED mid-test to confirm clean recovery (auto-fires resumed on schedule with no duplicate/missing jobs).
   - **Post-Phase-8 bugfix (real, user-reported)**: "schedule fires, then I hit Play Now on the same schedule - I get the queued toast, but it never plays a second time." Root cause, found via added temporary diagnostic logging plus isolated OSC spike scripts against the real workspace: `cue_cache`'s cached duration can be a hair shorter than QLab's true wall-clock playback (fade tails/rounding), so the fallback timer could free a zone and immediately retrigger the same cue number *while QLab was still finishing the previous instance* - which QLab silently ignores (no denial, no error, just no second audible play). Per the user's explicit direction ("always confirm before fire... each trigger hits a queue, the queue engine decides if/when it can run, then actually fires"), rewrote admission in `zoneQueueEngine.js` to always live-query `getIsRunningByUniqueId` before ever sending the real OSC start, retrying every ~150ms up to ~20 attempts (~3s) if still reported running, firing anyway past that cap as a last-resort safety net (recorded as decision 3 in the ADR). This made `enqueue()` and the whole admission chain async; `fn_on_due`/`fn_play_now` updated to `await core.queue.enqueue(...)`. Also added, per the user's explicit ask for a second notification "when it actually fires": a bounded recent-events ring buffer (`getRecentEvents`/`GET /api/queue/events` on Node-RED), polled by a new FastAPI background task (mirroring the existing health poller) that emits a `queue_notification` SocketIO event - surfaced as a toast in `base.html` - specifically for entries flagged `afterQueue: true` (i.e. ones that were genuinely queued, not every routine on-time schedule tick, which would otherwise spam a toast every 30s). Live-verified with a throwaway `python-socketio` client: reproduced the exact original bug scenario twice - once needing a `confirm_wait` retry (QLab still reported running), once clearing on the first check - both correctly fired and both surfaced a "Queued Message Played" toast end-to-end. All 75 project unit tests pass (16 in `zoneQueueEngine.test.js`, rewritten for the now-async engine, including dedicated confirm-wait/retry/timeout-safety-net/afterQueue-flagging cases).
   - **Post-Phase-8 refinement**: user reported cue 103 consistently taking precedence over an earlier schedule when both are due in the same zone at the same time, correctly suspecting it was arrival order (effectively cron-plus's internal job-processing order), not anything intentional. Changed the same-zone tie-break in `_compareEntries` from "entry id string" to "cue number ascending" - lower cue number now plays first - scoped specifically to entries due within the same wall-clock second (`Math.floor(dueAt/1000)` equal, tolerating cron-plus's few-ms dispatch jitter between two schedules genuinely configured for the same moment); entries due in different seconds are unaffected and still order strictly by time.
     - **This alone shipped as a real bug, caught live before the user had to report it again**: watching the actual Node-RED log showed cue 103 was still winning, because it reached `enqueue()` ~2ms before cue 101 and got admitted into the free zone *immediately* - before 101 even existed to be compared against, so the new comparator never got a chance to run. Per the user's explicit choice (offered a ~200ms vs a shorter ~50-75ms option), added a short admission settle window (`admissionSettleMs`, default 75ms): admission into a newly-free zone now always waits out this window before picking a winner, giving a near-simultaneous sibling schedule time to arrive and be sorted in. Hit and fixed a second bug while building this: the settle timer's own completion callback called `_tryAdvance()`, which immediately restarted *another* settle window for the same still-unresolved zone, looping forever without ever admitting anything - fixed by adding a `_settled` marker that's only cleared once the zone is actually occupied again or its queue empties, so a "contest" only settles once. Also fixed a related hang: `preemptZones` clearing a settle timer without resolving its promise would leave any `enqueue()` call awaiting that zone hanging forever - now resolves it so the caller correctly sees the entry as queued/dropped instead of never returning. Recorded as decision 5 in `docs/adr/0001-zone-queue-tiebreak-policy.md`, including the explicit tradeoff that this adds a small fixed delay to every fire through a zone (not just contested ones), accepted as negligible for both routine announcements and future VOG triggers.
     - Added 5 new dedicated tests (settle window lets a later-arriving lower cue number still win a real race into a free zone; a lone uncontested entry still fires after the window elapses; `preemptZones` unblocks a pending settle wait instead of hanging; same-second collision resolved by cue number regardless of arrival order; a different-seconds case confirming due time still wins over cue number when it's not really a collision). All 80 unit tests pass. Live-verified against the real running instance: watched schedule 1 (cue 101, every 30s) and schedule 9 (cue 103, every 90s) collide at their 90s alignment point - cue 101 now fires first as expected, and cue 103 still fires immediately afterward (flagged `afterQueue: true`, so the operator also gets the delayed-fire toast for it), confirming nothing is dropped, just correctly reordered.
     - **A third, related bug, found immediately after by the user testing a genuinely multi-zone cue** (schedule 1/cue 101/Zone 1, "Cue 2 Test"/cue 102/Zone 1+2, schedule 9/cue 103/Zone 2 all aligned): cue 103 played *last*, after both 101 and 102, even though Zone 2 was completely free the whole time it was waiting. Root cause: admission required an entry to be literally at the front (index 0) of *every* zone's queue array it touched. Since cue 102 (Zone 1+2) sorted ahead of cue 103 (Zone 2 only) in Zone 2's queue purely because 102 < 103, and `_findReadyEntry` only ever inspected index 0, cue 103 was never even considered - even though it needed nothing from Zone 1 and Zone 2 was sitting idle. Before fixing, checked with the user whether "independent per-zone processing" could even mean literally splitting one cue's audio to start at different times per zone - clarified that's not physically possible (`/cue/{n}/start` plays into every routed output at once, indivisibly), and confirmed the fix needed is scoped to the *scheduling* layer, not any change to the "wait for every zone" multi-zone semantics itself (decision 4, unchanged). Fixed `_findReadyEntry` to scan each free zone's queue for the first entry whose *every* zone is currently free, rather than gating on literal front-of-queue position - single-zone entries behind a stuck multi-zone entry can now be found and admitted, while FIFO/cue-number order among entries genuinely competing for the same zone is preserved (the scan still returns the first ready entry in sorted order). Added a dedicated regression test reproducing the exact three-schedule scenario. All 81 unit tests pass. Live-verified twice: once against residual occupancy from an auto-fire collision, once from a fully idle state - both times cues 101 and 103 fired independently and immediately while cue 102 correctly waited for both zones, then fired once, exactly matching the reported expectation.
9. ✅ **VOG** — done. `lib/vog/vogInterruptHandler.js` built as a plain async function (`triggerVog(deps, vogMessage)`, deps = `{ qlabProtocol, resolveZonesForCue, queueEngine }`) rather than a class, mirroring `refreshCueCache`'s style since it has no state of its own beyond orchestration: live-resolves the VOG cue's own zone scope via `resolveZonesForCue`, reads `queueEngine.getState()` to find every *distinct* confirmed occupant across those zones (deduped by cue number, so a multi-zone occupant only gets one `stopCue`), issues real OSC `stopCue` calls for each (best-effort - a rejected stop doesn't block the rest, per-call `.catch`), calls `queueEngine.preemptZones(zones)` to clear bookkeeping and drop anything still waiting there (no requeue), then `enqueue`s the VOG cue itself through the exact same `zoneQueueEngine` (so it gets the same confirm-before-fire/occupancy tracking as any other entry, accepting the same flat ~75ms settle-window delay as decision 5 already called out for future VOG triggers). A zero-zone VOG cue (fully muted/unrouted) skips the stop/preempt step entirely and still fires via the queue engine's existing zero-zone fast path. Wired into `lib/index.js`'s `createCore` as `core.vog.trigger(vogMessage)`; `fn_trigger_vog` in `node-red/flows.json` upgraded from its interim direct-`playCue` stub to call it, returning `{ status, fired, zones }`.
   - Unit tests (`test/unit/vogInterruptHandler.test.js`, 9 cases, deps fully mocked): stops every distinct confirmed occupant across the zone scope, dedupes a shared multi-zone occupant into one `stopCue` call, leaves a merely-reserved/mid-confirm occupant alone (nothing to stop yet), ignores occupancy outside the VOG cue's own scope, preempts before enqueueing (call-order asserted), enqueues with live duration/uniqueId, tolerates a `stopCue` rejection without blocking the rest, zero-zone fast path, and propagates `fired:false` if the queue engine reports the VOG entry as queued. All 90 unit tests passed at this point.
   - Live verification against the real running Node-RED/QLab instance: triggered the existing all-zone VOG message (cue 104, Zones 1+2) while cues 101 (Zone 1) and 103 (Zone 2) were both actively playing from manual `play-now` calls - the Node-RED log showed real `/updates` pushes confirming both were actually silenced (QLab's own push in reaction to the real OSC `/stop` commands `triggerVog` sent, not just bookkeeping) before cue 104 fired into both zones, and the three schedules that piled up in the queue during VOG's ~11.7s playback all correctly fired afterward flagged `afterQueue: true` (nothing dropped or stuck). Also created a disposable temporary VOG message scoped to Zone 1 only (reusing cue 101) and confirmed a single-zone VOG leaves an unrelated zone's occupant (cue 103, still playing in Zone 2 the whole time) completely undisturbed - no `stopCue` call, no zone-freed event for Zone 2 until its own natural duration elapsed. Deleted the temporary VOG message afterward.
   - **Real bug caught by this same live testing, not by guesswork**: during the single-zone VOG test, the log showed an entry (a `play-now` reserved into Zone 1 moments before the VOG trigger's `preemptZones` ran) still firing - calling the real OSC `playCue` - *after* `preemptZones` had already cleared that zone's occupancy for the VOG cue. Root cause: `preemptZones` only deleted the occupancy map entry; it never stopped the *in-flight* `_confirmClearAndFire` confirm-retry chain already running for whatever was reserved (but not yet fired) in that zone, so that chain would complete obliviously and call `_fire()` anyway - physically racing the VOG cue for the same zone. Fixed by having `preemptZones` mark the reserved entry `_preempted = true` when clearing its occupancy slot, and `_confirmClearAndFire` checking that flag both up front and again immediately before calling `_fire`, emitting `preempted_before_fire` and abandoning the fire if set. Recorded as an addendum to decision 9 in `docs/adr/0001-zone-queue-tiebreak-policy.md`. Added a dedicated deterministic unit test (a controlled pending `getIsRunningByUniqueId` promise held open while `preemptZones` runs, then resolved) since real-world timing can't reliably be made to hit this exact window on demand - the live run that first surfaced it was a lucky catch, not a repeatable manual test. All 91 unit tests pass after the fix; re-ran the live single-zone scenario afterward as a smoke check (clean, no anomalies, though the race window wasn't hit again that time - expected, given it's timing-dependent).
10. ✅ **Health/history pages + polish** — done. `/status` and its SocketIO wiring (`app/main.py`'s `_poll_and_broadcast_health` background task, `status.js`'s `health_update` listener) and the `/history` page template were already built during the Phase 6 scaffold - what Phase 10 actually closed was the real gap the `/history` template's own empty-state message pointed at: `lib/log/eventLogger.js` didn't exist yet, so `logs/events-YYYY-MM-DD.log` was never written and the page had nothing to show. Built it now: `winston` + `winston-daily-rotate-file` (already-pinned deps, unused until this phase), 30-day retention, one plain-text line per business event, `fs.mkdirSync(logDir, { recursive: true })` up front (mirrors `lib/db/database.js`'s same pattern for `data/`) since a fresh checkout has no `logs/` dir. Exposes two functions rather than a class (no state of its own beyond the winston instance): `logQueueEvent(event, entry, extra)` - same signature as `zoneQueueEngine`'s `onEvent` hook, so it drops straight into `onQueueEvent` with no adapter - and `logHealthTransition(from, to)`, wired to `healthMonitor`'s `stateChange` event (`core.health.on('stateChange', ...)` in `node-red/settings.js`) so disconnect/reconnect cycles land in the same operator-facing history as queue events, not just Node-RED's own console. `node-red/settings.js` now builds one `eventLogger` instance and passes `eventLogger.logQueueEvent` as `onQueueEvent` directly, replacing the bare `console.log` placeholder from Phase 8.
    - Unit tests (`test/unit/eventLogger.test.js`, 7 cases, plain mock logger - no filesystem touched): field formatting for a `fired` event with entry + extra, omitting `extra` entirely when none is passed, omitting undefined/null entry fields rather than printing them literally, `source: 'vog'` showing up for VOG-originated entries, and both `health_reconnect`/`health_disconnect` transition formatting (including the initial `unknown -> disconnected` case). All 98 unit tests pass.
    - Live verification against the real running instance: restarted Node-RED, confirmed `logs/events-2026-07-20.log` was created automatically and immediately received a real `health_reconnect from=unknown to=connected` line on startup; fired a schedule and a play-now and confirmed `queued`/`fired`/`zone_freed` lines appeared with correct fields (including `afterQueue=true` for the play-now that genuinely waited); confirmed `GET /history` on the FastAPI webapp renders these exact lines directly (most-recent-first, HTML-escaped) with no proxy involved, matching the plan's "webapp reads the log file directly" design.
    - **Startup-gate verification, done live by the user rather than simulated**: quit and relaunched the real QLab app mid-session. The event log shows the complete real cycle - `health_disconnect from=connected to=disconnected` at 11:57:40.800Z, then a ~41s gap with **zero** `queued`/`fired` lines (confirming `fn_on_due`'s `core.health.isArmed()` check correctly skipped every schedule due during the outage rather than queuing or replaying them), then `health_reconnect from=disconnected to=connected` at 11:58:21.895Z, with normal schedule fires resuming immediately and cleanly on the very next tick - exactly the "stay disarmed until QLab is confirmed live" startup-gate behavior from the plan, confirmed end-to-end via real log entries from a real QLab restart rather than `healthMonitor`'s existing fake-timer unit tests alone.
    - **Post-Phase-10 polish, user-requested**: the "Refresh Cue Data" button in the shared `page_header` macro appeared on every page, including `/history` where it's meaningless (it re-reads QLab cue duration/zones, not the event log). Made it opt-out (`page_header(..., show_refresh_cues=True)`, default unchanged everywhere else) and passed `False` from `history.html`, replacing it with a page-specific "Refresh Log Entries" action button plus periodic auto-refresh (every 5s) so the page updates on its own without a manual reload. Extracted the log-tailing logic (glob for the latest `events-*.log`, read last 200 lines, reverse) out of `pages.py`'s `history_page` handler into a shared `app/log_reader.py` (`read_recent_entries()`), used by both the server-rendered first load and a new `GET /api/history/entries` JSON endpoint (`app/routers/history_api.py`) that the page polls afterward - a plain re-read of the same file, not a Node-RED proxy. New `static/js/history.js` renders entries client-side (mirroring the server template's `.log-entry`/`.text-vog`/`.text-danger` classing and the `.empty-state` markup exactly, HTML-escaped) and drives both the interval poll and the manual button via the existing `ButtonStateManager`. Verified live: `/history` no longer renders `refreshCuesBtn` while `/schedules`, `/vog`, and `/status` still do (each exactly once); `GET /api/history/entries` returns real current log lines; the page's initial server render and the JSON endpoint's data match exactly, confirming both paths read the identical file the identical way.
11. ✅ **Soak/reliability pass** — done (per the user's explicit call: no formal multi-day soak, given how much real-world testing already happened live across every phase; that item stays open in the checklist for future reference rather than being pretended away).
    - **Edge-case audit before the mechanical checklist work**: read (not live-tested) through the system for gaps unit tests wouldn't catch. Found and discussed with the user: (1) `fn_startup` never reconciles in-memory zone occupancy with what's actually still playing in QLab, so a Node-RED restart mid-playback could let an immediately-due schedule collide with real audio the engine has no record of; (2) deleting a schedule doesn't purge its already-queued (not yet fired) entry, so a deleted schedule can still fire once more; (3) `fn_play_now` never checks the `enabled` flag, unlike VOG's trigger endpoint. Per the user's explicit call, none of these three were acted on this pass - flagged and left as accepted/understood behavior rather than fixed speculatively.
    - **Fixed (the one the user asked to act on): a denied/failed OSC `/start` no longer holds its zone hostage for the full assumed duration.** `_fire()` claims zone occupancy (a timer for the cue's real cached duration, or the 30s fallback if unknown) *before* even calling `playCue()`; previously, a rejected `playCue()` only logged an `error` event and left that timer running to completion regardless - so a denied start (e.g. OSC control permissions toggled off mid-session, or the cue deleted/renamed in QLab between the `cue_cache` refresh and the fire - narrower than "any bad cue number," since a wholly nonexistent one fails earlier in `refreshCueCache` and never reaches `enqueue()`) wasted the whole window locking out a zone for a cue that never made a sound. Fixed by freeing every one of the entry's zones immediately in the rejection handler, tagged `start_failed_zone_freed` (distinct from a normal `zone_freed`) so the event log can tell "burned its slot without playing" apart from a completed playback. Recorded as an addendum to decision 2 in `docs/adr/0001-zone-queue-tiebreak-policy.md`.
      - 4 new unit tests: zone frees immediately (not after the full duration) on a denial, with both `error` and `start_failed_zone_freed` emitted; a multi-zone entry frees *every* one of its zones, not just one; a later entry in that same zone fires right away instead of waiting; and a race guard (zone already reclaimed by a preempt before the stalled `playCue()` rejection resolves) doesn't double-free or throw. All 101 unit tests pass. Not live-tested against real QLab - reproducing a genuine OSC-level denial would mean toggling QLab's own OSC permission settings mid-session, which is more disruptive than warranted for a fix this contained and already exercised deterministically at the unit level.
    - **`test/integration/INTEGRATION_CHECKLIST.md`** created, consolidating everything already verified live across every phase (OSC permissions, connectivity, collision paths, VOG, health disarm/rearm, restart correctness, free-tier nag risk) plus what's still open (launchd install itself, the multi-day soak, and a production-workspace re-check once the real venue workspace is in use).
    - **`deploy/launchd/`** created: two per-user LaunchAgent plist templates (`RunAtLoad`+`KeepAlive` for both Node-RED and `uvicorn`, hardcoded paths for this machine, no ordering dependency between them since the webapp already degrades gracefully if Node-RED isn't up yet) plus a README with install/uninstall commands. Validated with `plutil -lint`. **Not installed** - per the user's explicit choice, left as templates for later rather than modifying real login-time behavior now.
    - **Restart-recovery stress test, done live**: 5 consecutive real Node-RED restarts and 3 consecutive real `uvicorn` restarts. Caught a real methodology bug mid-test - the first kill attempt's `pkill -f` pattern matched nothing because Node-RED sets its own process title (so the literal script path never appears in `ps`'s command column), silently leaving the original process alive while 4 of 5 "restarts" were actually just re-hitting it; fixed by killing whatever process actually holds the port instead of pattern-matching argv (and, for `uvicorn`, discovered `lsof -i:PORT` alone can match an unrelated process with an established connection *to* that port - e.g. a stray Chrome/Google socket - so the fix specifically filters to the LISTEN-state socket owner only, never touching anything else holding a connection through that port). With that fixed, every one of the 5 real Node-RED restarts logged an identical `Startup: rebuilding 4 cron-plus directive(s)` (1 remove-all + 3 enabled schedules, idempotent by construction since `cronSync.rebuildAll` always wipes every dynamic job first) and came back `connected`/`armed` immediately; the event log shows exactly 5 `health_reconnect` lines and zero duplicate/orphaned queue entries across the whole window. All 3 `uvicorn` restarts returned 200 on every page immediately afterward.

12. ✅ **Patch-based zone-derivation rework + music ducking** (post-Phase-11, driven by a real QLab-side rework) — done. The operator moved to a paid QLab Audio license and reworked audio routing: each physical output is now fed by two Audio Patches (one Messaging, one Music), one dedicated Messaging Patch per zone, plus continuous per-zone background music that must be ducked while a message plays. This broke the crosspoint-matrix zone-derivation mechanism outright (a cue's zone identity is now "which Audio Patch is it assigned to," not a gain-matrix reading), and an initial QLab-side attempt at ducking via a Fade-cue-then-follow-chain was tried and abandoned (broke the fired cue's own zone/duration signal and the "cue we fired = cue we track" identity everything else depends on).
    - **Live OSC spike** (mirroring Phase 0's methodology, since the QLab docs site wasn't fully fetchable): confirmed `/cue/{n}/patch` returns a 1-based integer index into `/settings/audio/patchList` (not a uniqueID/name); confirmed `/settings/audio/patchList` (not `/audioPatches`, which doesn't exist); confirmed `/cue/{n}/levels` still replies but returns a large, zone-irrelevant matrix now (safe to delete); confirmed Group cue children nest via `/cueLists` exactly as before, with one real wrinkle caught live - Group children initially had no cue number at all, only a `uniqueID` (the operator subsequently assigned them real numbers, simplifying the resolver to one uniform addressing scheme). Findings written to `test/fixtures/qlab-osc-findings.md`. Hit a real connectivity gotcha mid-spike: a standalone spike script got zero OSC replies even with Node-RED stopped; routed queries through Node-RED's own already-connected socket instead via a temporary, narrowly-scoped (fixed hardcoded addresses, never a caller-supplied one) debug endpoint, removed immediately after.
    - **`lib/zones/zoneResolver.js` rewritten**: `parseLevelsMatrix`/`SILENCE_FLOOR_DB` deleted outright (no fallback); leaf Audio cues resolve to a zone via patch assignment (`resolveZoneForLeafCue`), Group/Cart cues resolve as the recursive union of their children's own zones (`resolveZonesForCue`, reusing the existing `/cueLists` tree fetch, no new OSC call). `qlabProtocol.js` gained `getCuePatch`/`getAudioPatches`, lost `getLevels`. New `lib/zones/audioPatchMap.js` (replaces `zoneMap.js`) loads `config/audio-patch-map.json` (replaces `zone-map.json`): per-zone Messaging Patch id plus duck/unduck cue numbers, fail-fast validated (missing fields, duplicate patch ids). `lib/index.js`/`node-red/settings.js` rewired accordingly.
    - **Real post-rework bug caught by the user, not by testing**: the webapp showed all four test schedules targeting *both* zones. Root cause: Node-RED's live process hadn't been restarted since the code landed, so it was still running the old crosspoint logic against the *new* patch-based `/levels` payload shape, which happened to spuriously read every channel as "open." Fixed by restarting Node-RED and re-running the cue-cache refresh; confirmed each schedule's `cue_cache.zones` corrected to its own single zone.
    - **Ducking, moved into the engine rather than QLab** (the operator's own proposal, adopted over the initial Fade-cue approach): message cues keep real cue numbers and fire directly, exactly as before this whole rework - restoring full compatibility with every existing duration/`/updates`/occupancy assumption with zero changes to cue-identity tracking. Ducking becomes two static per-zone QLab cues (`duckCueNumber`/`unduckCueNumber` in `config/audio-patch-map.json`), fired via the same `playCue()` mechanism as anything else. `lib/queue/zoneQueueEngine.js` gained a new `onZoneTransition('duck'|'unduck', zone)` hook (deliberately separate from the existing passive `onEvent` hook), firing duck once per burst (zone empty→occupied, from `_tryAdvance`'s admission loop) and unduck once the zone is confirmed fully drained (`_isZoneFullyIdle`, checked only from `_freeZone` and the settle-window callback, always *after* `_tryAdvance()` has had a chance to re-admit a waiting sibling - this ordering is what prevents an audible duck/unduck flicker between back-to-back queued messages). Modeled as a direct side-channel hook rather than pushing duck/unduck through the normal admission/settle-window path, to avoid reopening the exact race classes already hit and fixed twice in that file (see ADR decisions 5/9).
    - **`lib/vog/vogInterruptHandler.js`**: VOG ducks immediately (bypassing the settle window, consistent with its existing urgency) via a new `duckImmediately` dep, then calls a new `queueEngine.markDucked(zones)` so the engine's own idle-check still owns the eventual unduck - keeping exactly one unduck decision path total.
    - **Startup validation**: `fn_startup`'s existing cue-cache warm-up sweep now cross-references every zone a real schedule/VOG cue actually resolves to against `config/audio-patch-map.json`, logging loudly (`node.error`) if any zone has no duck/unduck config. Deliberately *not* a thrown exception (a deviation from the original plan wording) - throwing there would land after cron-plus's rebuild directives are computed but before they're returned/applied, so one zone's config gap would disarm every schedule in every zone, not just the misconfigured one.
    - **ADR addendum**: decision 10 added to `docs/adr/0001-zone-queue-tiebreak-policy.md` covering the abandoned Fade-cue approach, the batching rule, the side-channel-hook-vs-queue-entry choice and why, and the VOG-immediate-duck-plus-`markDucked` reconciliation.
    - 20 new unit tests across `audioPatchMap.test.js` (new), `zoneResolver.test.js` (rewritten - leaf/unmapped-patch/Group-union/nested-Group/same-zone-dedup cases), `qlabProtocol.test.js`, `zoneQueueEngine.test.js` (solo duck/unduck, the back-to-back-no-flicker regression specifically, a settle-window race, `preemptZones`-doesn't-unduck, zero-zone-never-ducks, `markDucked`), and `vogInterruptHandler.test.js` (duck-before-enqueue ordering, `markDucked` ordering, zero-zone skip, duck-rejection tolerance). All 126 project unit tests pass. Ducking itself not yet live-tested against real QLab at time of writing (unlike the zone-derivation half, which was live-verified via the cue_cache-correction bug above) - unit-level only so far.
    - **Post-Phase-12 refinement, driven directly by live testing**: the operator reported the message cue firing immediately after (overlapping) the duck cue, and asked whether the duck cue's own live-queried duration could be accounted for as a real playout duration. The first attempt padded the *fallback* occupancy timer by the duck cue's duration - live testing immediately showed this had no real effect, since the zone almost always frees via the *primary* mechanism (a real `/updates` push confirming the message cue itself stopped), which is entirely independent of the fallback timer's value. Per the operator's own steer ("treat the duck like a message audio cue... the message cue has to wait until the duck finishes AND the zone is not freed until the unduck cue finishes"), replaced with a genuine block-on-completion model instead: `zoneQueueEngine.js`'s `_beginAdmission` now awaits the zone's duck cue completing (via a new `lib/zones/duckDuration.js`'s `playCueAndWaitForDuration` - plays the cue, then live-queries and waits out its own duration) *before* the message's own confirm-before-fire chain ever runs, and `_maybeUnduck` reserves the zone with a synthetic occupancy placeholder the instant it decides to unduck (blocking new admission, letting a genuinely back-to-back arrival queue behind it) until the unduck cue is confirmed done too. Neither depends on guessing at durations for correctness anymore - both await real completion, the same discipline the message's own confirm-before-fire already used. Removed the now-superseded fallback-timer-padding code and its `resolveDuckDurationSeconds`/`duckDurationSeconds` wiring through `fn_on_due`/`fn_play_now`. 20 unit tests updated/added (duck genuinely blocks the message; a rejected duck hook doesn't block firing; preemption mid-duck-wait and mid-unduck-wait handled cleanly; back-to-back-no-flicker and settle-window-race regressions re-verified under the new model). All 135 unit tests pass. Live-verified end-to-end: a real play-now on cue 1101 showed a genuine ~2.5s gap between `duck_wait` and `fired` (Zone 1's duck cue's real duration, not an instant overlap), `zone_freed`/`unduck_wait` firing together once the message ended, and a subsequent play-now correctly re-ducking once the zone was truly clear. ADR decision 10 revised to describe the final mechanism and record the abandoned fallback-timer attempt as a documented amendment.
    - **Second post-Phase-12 refinement, again caught by the operator's own live testing**: a multi-zone Group cue's two zones only ever freed at the exact same timestamp regardless of each child's real duration, and any other message queued behind the group in the SHORTER zone waited for the whole group (i.e. the longer sibling zone) to clear too - both traced to the same root cause: a multi-zone entry shared one `durationSeconds` across every zone, sourced from the group's own `/duration`, which QLab reports as its longest child's, not any individual zone's. Fixed by resolving each zone to its own discrete duration instead: `zoneResolver.js`'s existing tree-walk now also tracks which specific leaf cue provides each zone (new `resolveZoneInfoForCue`), and a new `resolveDurationSecondsByZone` queries that child's own duration per zone rather than the group's. Wired through `fn_on_due`/`fn_play_now`/`vogInterruptHandler` as `entry.durationSecondsByZone`; `zoneQueueEngine.js`'s `_fire()` now computes each zone's own timer duration inside its per-zone loop instead of once, shared, outside it - a plain single-zone cue is unaffected either way. No change needed to the early-`/updates`-confirm race (decision 2) - a short zone's own accurate timer now always wins that race naturally, without needing per-zone uniqueId tracking. 5 new unit tests; all 144 pass. Live-verified: the same real 2-child group's zones now free ~2.5s apart matching each child's actual length, and a Zone-1-only message queued behind it fired the instant Zone 1 alone cleared - correctly still recognized as the same continuous burst (no premature unduck) rather than waiting on Zone 2. ADR decision 10 amended a second time to record this.
    - **Third post-Phase-12 refinement**: the operator asked about an audible ~half-second gap between a duck cue's audio actually finishing and the message cue starting. Root cause: `qlabProtocol.playCue()` calls `requestOptionalReply` with a 500ms timeout, since QLab is silent on a SUCCESSFUL `/start` (only replies on denial) - so a successful `playCue()` always takes its full 500ms to resolve. `duckDuration.js`'s `playCueAndWaitForDuration` awaited that 500ms call before even querying the duck cue's duration and starting the wait countdown, so every duck (and unduck) wait was padded by ~500ms plus a `getDuration` round trip beyond the cue's real audio length - the audio itself started near-instantly when the OSC message was sent. Fixed by firing `playCue` and querying `getDuration` concurrently instead of sequentially, and computing the remaining wait from when the play command was actually sent (`remainingMs = durationSeconds*1000 - (now() - startedAt)`) rather than adding the full duration on top of both round trips. 2 new unit tests in `duckDuration.test.js`; all 146 unit tests pass. Live-verified: restarted Node-RED, triggered cue 1101/Zone 1 twice, and confirmed the `duck_wait`-to-`fired` gap dropped from a consistent ~2.508s (multiple pre-fix log samples) to a consistent ~2.004s (multiple post-fix samples) - a ~504ms reduction matching the removed 500ms tax almost exactly. ADR decision 10 amended a third time to record this.
    - **Critical bugfix (unrelated to ducking, despite the operator's initial suspicion)**: the operator reported that when every schedule aligned to the same trigger moment, only the FIRST message fired - the rest never even appeared in the queue. Reproduced live via 5 concurrent play-now calls: only one returned/fired; the other 4 HTTP requests hung indefinitely (2+ minutes, well past any real timeout), with Node-RED's own log showing `Error: OSC request timed out waiting for /reply/cueLists` for each. Root cause: `resolveDurationSecondsByZone` (added by the Second post-Phase-12 refinement above, for per-zone Group durations) starts by re-fetching the whole cue tree via `qlabProtocol.getCueLists()` - an address with no cue-number scoping, so every concurrently-firing schedule issues an identical OSC query at once. Confirmed live that QLab answers only ONE of several simultaneous identical-address queries, leaving the rest to time out after 3s and reject. Unlike `refreshCueCache`'s equivalent query (already wrapped in a try/catch, degrading gracefully to a skip-with-warning), `fn_on_due`/`fn_play_now`/`vogInterruptHandler` all call `resolveDurationSecondsByZone` directly with no error handling - the rejection propagated uncaught, silently dropping the schedule's fire entirely (`fn_on_due`, no `queued` event ever emitted) or leaving the HTTP response never sent at all (`fn_play_now`, hence the hang). Fixed at the single source: `zoneResolver.js`'s `resolveDurationSecondsByZone` now catches a failure of its initial tree-fetch/zone-resolution step and returns `{}`, extending the exact same best-effort tolerance it already had for a single per-zone duration query failing. Safe specifically because its return value is only ever an optional per-zone override on top of an entry's already-resolved `zones`/`durationSeconds` - a caller getting `{}` back just falls through to the shared `durationSeconds` (the pre-per-zone-refinement behavior), never a dropped or duplicated fire. Deliberately did NOT extend this same tolerance to `resolveZonesForCue`/`resolveZoneInfoForCue` themselves, since those determine real zone-safety - silently returning empty zones on a transient failure would make it indistinguishable from a legitimately zero-zone cue, which fires immediately with no collision protection. 1 new unit test; all 147 unit tests pass. Live-verified: restarted Node-RED, re-ran the exact 5-concurrent-play-now reproduction - all 5 now return real HTTP 200s with no hangs, all 5 log `queued`, and all 5 fire/free cleanly in FIFO order. ADR decision 11 added to record this as a distinct, critical issue (not folded into decision 10's ducking amendments, since it has nothing to do with ducking).
    - **Follow-up fix, found by the operator immediately after**: with the above fix in place, two schedules in two completely UNRELATED zones aligning (e.g. Zone 1's cue 1101, Zone 2's cue 2101) showed Zone 2 not ducking/playing until AFTER Zone 1's message had already started - despite the zones having nothing to do with each other. Root cause: "gracefully degrading instead of hanging" still meant the losing schedule's `resolveDurationSecondsByZone` call burned the full 3000ms OSC timeout before proceeding - confirmed via log timestamps, one schedule queued immediately, the rest queued ~3.0s later every time. Since a duck cue's real duration (~2s) is shorter than that 3s penalty, the "losing" zone's whole duck+fire cycle hadn't even started by the time the "winning" zone's message was already playing. Fixed the actual race instead of continuing to soften its failure: `qlabProtocol.js`'s `getCueLists()` now de-dupes concurrent in-flight calls into one shared OSC round trip, since `/cueLists` is a single, cue-number-agnostic address every zone/duration resolution needs regardless of which specific cue it's after - every concurrent caller in the same window now gets the identical real tree back from ONE query instead of each racing for their own. 3 new unit tests in `qlabProtocol.test.js`; all 150 unit tests pass. Live-verified: re-ran the two-independent-zone reproduction - both zones `queued`/`duck_wait`/`fired` within 1-4ms of each other now; re-ran the 5-schedule reproduction too - all 5 `queued` within 2ms of each other (previously one immediate + four ~3.0s later). ADR decision 11 amended with this follow-up.

## Verification

- **Unit tests (Jest, `lib/` only, no live QLab):** `occurrenceCalculator` (interval/weekday/date-range/DST edge cases — done), `zoneResolver`'s pure matrix parser (fixture-driven — done), `zoneQueueEngine` (fake-timer: free-fires-immediately, occupied-queues, same-schedule stale-drop, cross-schedule FIFO, VOG preempt), `healthMonitor` (missed-heartbeat threshold, explicit disconnect, reconnect — done), `cronSync` (done), `scheduleModel` (done), repositories (done).
- **Node-RED internal API (hands-on via `curl`, same verification discipline used throughout Phases 0-3):** every endpoint in the Frontend Architecture table — confirm request/response shapes directly, confirm a write actually updates both SQLite and the live cron-plus job, confirm `127.0.0.1`-only binding (a request from the LAN-facing interface should fail).
- **FastAPI webapp:** unit/integration tests around `db/queries.py` (read shapes match `schedulesRepo`/etc.) and `node_red_client.py` (connect-refused → clean error envelope, not a crash); manual browser walkthrough of every page in Repo Structure's page list, since visual/UX correctness still needs a human — but with a much smaller, well-understood surface (plain Jinja2/Bootstrap/vanilla JS) than Dashboard 2.0's opaque widget internals.
- **Integration checklist (hands-on against real dev-machine QLab, not automatable):** confirm QLab's Workspace Settings → Network → OSC Controls has read/edit/control permissions enabled with no passcode (Phase 0 found this denies *every* OSC command, including basic cue triggering, when left at QLab's default — easy to forget on the production machine's workspace); OSC connectivity + keepalive past 61s; duration/levels accuracy on real cues; live cue-list enumeration; wall-clock single-schedule firing; same-zone collision via both the duration-timer path and a manually-killed cue's `/updates` path; VOG stop+play+non-resume; health-monitor disconnect/reconnect and scheduler disarm/rearm; Node-RED restart correctness (no duplicate/missing cron-plus jobs); launchd unattended auto-start for *both* processes with correct disarmed-until-confirmed behavior; and the free-tier nag-dialog risk from Phase 0.

## Confirmed: QLab version and queue cap

- **Target is QLab 5** — matches all OSC research/citations in this plan; no re-verification needed going into Phase 0.
- **Zone-queue overflow cap stays at 5 entries per zone** (oldest dropped + logged beyond that), as specified in the Zone-Queue Tie-Break Policy above.

### Critical files
- `lib/index.js`
- `lib/queue/zoneQueueEngine.js`
- `lib/osc/qlabProtocol.js`
- `lib/zones/zoneResolver.js`
- `lib/scheduling/cronSync.js`
- `lib/db/migrations/001_init.sql`
- `node-red/settings.js` (add `uiHost: '127.0.0.1'`)
- `node-red/flows.json` (delete Dashboard 2.0 nodes, add the internal API tab)
- `webapp/app/main.py`, `webapp/app/node_red_client.py`, `webapp/app/db/queries.py`
- `webapp/templates/base.html`
- `/Users/ryanthompson/Documents/_dev/UPS-Mgmt/templates/base.html` and `static/js/utils/api-client.js` (reference/port source, not part of this repo)
